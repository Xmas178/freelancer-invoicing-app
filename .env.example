# ====================================
# FREELANCER INVOICING APP - Environment Variables
# ====================================
#
# SETUP INSTRUCTIONS:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values (never commit .env to Git!)
# 3. Generate NEXTAUTH_SECRET: openssl rand -base64 32
# 4. For Docker: set DB_PASSWORD to a strong password
#
# ====================================

# ====================================
# DATABASE CONFIGURATION
# ====================================
#
# OPTION 1: SQLite (Local Development - Simple)
# - Good for: Local testing, development
# - No Docker needed
# - Database file stored in: prisma/dev.db
DATABASE_URL="file:./dev.db"

# OPTION 2: PostgreSQL (Production - Docker)
# - Good for: Production deployment, team development
# - Requires: Docker running
# - Command: docker-compose up -d
# Uncomment and use this for Docker:
# DATABASE_URL="postgresql://invoicing_user:your_password_here@localhost:5432/invoicing_db"

# PostgreSQL Password (only needed for Docker)
# - Used by docker-compose.yml
# - Generate strong password: openssl rand -base64 24
DB_PASSWORD="secure_password_change_me"

# ====================================
# AUTHENTICATION (NextAuth.js)
# ====================================

# NextAuth Secret Key - REQUIRED
# - Used for: JWT token signing, session encryption
# - Generate with: openssl rand -base64 32
# - MUST be different in production!
# - Keep this SECRET - never share or commit!
NEXTAUTH_SECRET="your_secret_key_here"

# NextAuth URL - REQUIRED
# - Local development: http://localhost:3000
# - Production: https://yourdomain.com
# - Update when deploying!
NEXTAUTH_URL="http://localhost:3000"

# ====================================
# DEPLOYMENT CHECKLIST
# ====================================
# Before deploying to production:
# [ ] Generate new NEXTAUTH_SECRET
# [ ] Update NEXTAUTH_URL to production domain
# [ ] Use PostgreSQL DATABASE_URL (not SQLite)
# [ ] Set strong DB_PASSWORD
# [ ] Never commit .env file to Git
# [ ] Add all variables to your hosting platform (Vercel/Railway/etc)